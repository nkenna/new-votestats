import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddvoterPage } from './addvoter';

@NgModule({
  declarations: [
    AddvoterPage,
  ],
  imports: [
    IonicPageModule.forChild(AddvoterPage),
  ],
})
export class AddvoterPageModule {}
